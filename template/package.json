{
  "name": "template",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "wasm:dev": "nodemon --watch ./wasm/assembly -x \"npm run build --prefix wasm\"",
    "dev": "concurrently \"npm run wasm:dev\" \"npm run start --prefix www\""
  },
  "keywords": [],
  "author": "",
  "devDependencies": {
    "concurrently": "^8.0.1",
    "nodemon": "^2.0.22"
  }
}
